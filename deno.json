{
  "name": "@devcapsule/adapter",
  "version": "3.2.0",
  "license": "ISC",
  "description": "Empowering Next-Generation UI Style with ES6 and WebComponent",
  "tasks": {
    "docs:sync-skill-references": "uv run python scripts/sync_skill_references.py",
    "docs:build": "deno task docs:sync-skill-references && uv run --group docs mkdocs build",
    "docs:serve": "deno task docs:sync-skill-references && uv run --group docs mkdocs serve --livereload",
    "test:esbuild": "deno run --watch-hmr --allow-all ./esbuild/test.js",
    "test:html": "engrave server test-src/ docs/test/ --watch-add='.*.js'",
    "test": "deno task test:esbuild & deno task test:html",
    "dist:typescript": "npx tsc --project tsconfig.json",
    "dist:esbuild": "deno run --allow-all esbuild/dist.js",
    "dist": "deno task dist:typescript & deno task dist:esbuild",
    "publish": "deno publish --allow-slow-types --allow-dirty"
  },
  "compilerOptions": {
    "lib": [
      "dom",
      "dom.iterable",
      "dom.asynciterable",
      "deno.ns",
      "deno.unstable"
    ]
  },
  "unstable": ["sloppy-imports"],
  "lint": {
    "rules": {
      "exclude": ["no-sloppy-imports"]
    }
  },
  "publish": {
    "include": [
      "src/",
      "dist/browser/",
      "skills/adapter/",
      "README.md",
      "LICENSE.md"
    ]
  },
  "exports": "./src/mod.ts",
  "imports": {
    "@devcapsule/adapter": "./src/mod.ts",
    "@std/fs": "jsr:@std/fs@^1.0.20",
    "@std/path": "jsr:@std/path@^1.1.3",
    "esbuild": "https://deno.land/x/esbuild@v0.27.0/mod.js"
  }
}
